## 1. 采集与原文存储（capture）

- [x] 1.1 建立原文 BLOB、来源分段和轮次索引，保留旧摘要。
- [x] 1.2 实现默认启用点、历史导入、归档去重和完整行增量事务。
- [x] 1.3 独立采集循环接入后台唤醒，排除自身 Codex 会话。

## 2. 转换与设置（conversion）

- [x] 2.1 实现唯一当前配置与设置保存、密钥遮蔽、两阶段开关阈值。
- [x] 2.2 接入 API/Codex 共用生成接口、固定输入和输入预算。
- [x] 2.3 手动/自动入队、失败重试与恢复共用 Runtime。

## 3. Wiki 与 Skill（author-install）

- [x] 3.1 Wiki 编辑、正文版本与回退支持并发检查，MCP 共用业务逻辑。
- [x] 3.2 Skill 安装完整目录，预览差异、确认覆盖、检查目标变化并记录恢复。

## 4. WebUI（webui）

- [x] 4.1 增加同源 HTTP 写操作和读取接口，Raw 与旧摘要区分展示。
- [x] 4.2 页面实现输入选择、转换、Wiki 编辑、设置、历史回退、失败重试和 Skill 安装。
- [x] 4.3 补齐 HTTP 与 React 操作测试，构建并用隔离数据检查页面流程。

## 5. 实验清理与交付（cleanup）

- [x] 5.1 按依赖关系删除实验专属代码、提示词、示例、测试、文档与依赖，保留业务模型能力。
- [x] 5.2 更新业务安装、采集范围、设置、历史迁移、转换和安装文档。
- [x] 5.3 完成规定测试及当前改动复核，核对全部规格。

## Verification plan

已有 Python unittest 覆盖 Store、Runtime、Codex、MCP、Skill 与 HTTP；React 使用 Vitest/Testing Library。新增采集、安装与模型适配测试放在对应职责文件，保留发布恢复和同源限制测试。验收映射：capture 覆盖原文、历史与自身排除；conversion 覆盖阈值、双模型和固定批次；author-install 覆盖 Wiki、历史和安装；webui 覆盖页面全部操作；cleanup 覆盖实验删除及整条流程。

预计自动检查最多 12 条命令：采集、Runtime/模型、Wiki/安装、HTTP/MCP 各一组定向测试，Python 剩余业务完整套件一次，前端测试一次、构建一次，OpenSpec 校验一次，依赖与导入检查一次，预留最多三条仅用于实际失败的定向复验。由于 Raw 数据和公共运行入口全面改变且删除实验共享引用，完整 Python 业务套件用于发现跨模块导入和持久化回归；前端完整套件覆盖共享数据类型和导航影响。真实 API/Codex 验证若无法运行，明确列为未验证；隔离协议桩与页面测试仍须完成。不能仅因预算剩余扩大测试。
