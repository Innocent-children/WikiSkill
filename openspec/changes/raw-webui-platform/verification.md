# 验证记录

## 当前结果

- Python 业务测试：43 项通过（`test_live_*.py`），原生模型客户端测试 3 项通过。
- 前端完整套件：12 项通过；页面后续变更后定向复验 Manage/Jobs 的 6 项测试通过。
- TypeScript 检查和 Vite 生产构建通过，资源已写入 Python 包。
- OpenSpec 严格校验通过；改动空白检查通过。
- 隔离浏览器已实际查看 Raw 原文，保存 Wiki 并看到新增历史，复制 Skill 到临时安装目录，再次安装出现同名差异确认。

首次 Python 套件运行有 3 个错误，均由 macOS 临时目录 `/var` 与 `/private/var` 的路径别名被误判引起。已在配置构造时规范化目录，安装与完整流程定向复验通过。后续复核补上旧摘要批次重试拒绝，避免旧观察 ID 被当成新轨迹 ID，并对当前业务包重新验证。

共执行 12 条自动检查命令：Python 初次完整套件 1、安装复验 1、完整流程复验 2、最终业务包 1、原生客户端 1；前端完整测试 1、定向复验 1、构建 2；空白检查 1、OpenSpec 校验 1。依赖安装和隔离页面启动为准备操作。全部测试使用临时数据和本地协议桩。

## 验收对应

| 内容 | 验证方式 |
| --- | --- |
| 字节原文、增量位置、历史导入、归档与未知事件 | CaptureTests |
| 手动/自动批次、固定输入、失败重试和并发 Wiki 修改 | LiveStoreTests、LiveRuntimeTests |
| API 与 Codex 调用接口 | ApiModelTests、GeminiTests、LiveCodexTests、独立子进程协议桩 |
| Wiki 编辑、历史、安装及同源限制 | WorkflowTests、LiveWebTests、浏览器操作 |
| Skill 完整目录快照与中断恢复 | LiveSkillsTests、InstallTests |
| 设置页面密钥遮蔽及表单 | HTTP 工作流测试、Manage 前端测试 |
| 删除实验后的业务导入与发行资源 | Python 测试导入、uv sync、前端生产构建 |

## 本次验收范围

用户明确要求：“将真实服务调用排除本次验收，然后完成任务。”本次验收采用已通过的本地协议测试、业务测试、前端测试、生产构建和隔离浏览器验证。真实 API/Codex 服务调用不作为本次交付的待完成项；产品仍保留两种执行方式。

本次仅调整验证范围说明，产品代码、规格行为和既有通过结果均未改变；不重复运行已经通过的检查。

## 未执行的范围外检查

没有对真实付费 API 或真实 Codex 模型执行生成调用。本次不对实际服务的生成效果、登录权限或配额作验证结论；已验证本地 API HTTP 协议和 Codex app-server 子进程协议、业务结果与恢复流程。

## 当前限制

Codex 轨迹格式变化需要适配；原文包含上游实际写出的内容，无法解密或补齐缺失记录。超出输入预算的批次明确失败并保留 Raw。现有配置需要按当前格式重新填写，旧摘要及历史保留，旧摘要批次仅供查看。
